# Postgres connection string (Vercel Postgres / Neon / Supabase all work)
DATABASE_URL="postgresql://user:password@host:5432/thanaweya?schema=public"

# Ads: Adsterra ad codes are configured directly in lib/adsterra.ts, no env vars needed.

# Site
NEXT_PUBLIC_SITE_URL="https://your-domain.com"
